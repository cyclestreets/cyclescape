"en-GB":
  activerecord:
    errors:
      models:
        issue:
          attributes:
            size:
              less_than: "of the issue is too large."
        user:
          attributes:
            full_name:
              blank: "Please enter a name"
              email: "Please enter an email address"
        message_thread:
          attributes:
            base:
              disabled: Cannot be created by a disabled user
            closed:
              non_subscriber: can only be changed by a subscriber
            privacy:
              must_be_public: This must be public unless the thread is owned by a group
    attributes:
      issue:
        external_url: External site
      message_thread:
        group: "Owned by"
    models:
      message_thread: Thread
