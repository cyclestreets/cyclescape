A new issue with the title "<%= raw @issue.title %>" has been reported
in the <%= @group.name %>'s area.

<%= raw @issue.description %>

You can view the issue at:
<%= issue_url(@issue) %>

--------
You have received this message because your preferences indicate that you
would like us to notify you when new issues are reported in your groups' areas.

To stop these notifications, edit your user preferences at:
<%= edit_user_prefs_url(@user) %>
