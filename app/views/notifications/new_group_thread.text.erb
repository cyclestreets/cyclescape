<% if @thread.has_issue? %>
A new discussion titled "<%= raw @thread.title %>" has been created on
issue "<%= raw @thread.issue.title %>" for the <%= raw @group.name %> group.
<% else %>
A new administrative discussion titled "<%= raw @thread.title %>" has
been created for the <%= raw @group.name %> group.
<% end %>

<%= raw @thread.first_message.created_by.name %> wrote:
<%= raw @thread.first_message.body %>

You aren't subscribed to this discussion thread, so you won't receive any
more updates. You can read the whole thread, and subscribe to it at:
<%= thread_url(@thread) %>

--------
You have received this message because your preferences indicate that you
would like us to notify you on matters in your group.

To stop these notifications, edit your user preferences at:
<%= edit_user_prefs_url(@member) %>
