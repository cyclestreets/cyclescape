<% if @thread.has_issue? %>
A new thread titled "<%= raw @thread.title %>" has been created on issue
"<%= raw @thread.issue.title %>" for the <%= raw @group.name %> group.
<% else %>
A new thread titled "<%= raw @thread.title %>" has been created for the
<%= raw @group.name %> group.
<% end %>

<%= raw @thread.first_message.created_by.name %> wrote:
<%= raw @thread.first_message.body %>

On thread:
<%= thread_url(@thread) %>

--------
You have received this message because your preferences indicate that you
would like us to notify you when a new group thread is started.

To stop these notifications, edit your user preferences at:
<%= edit_user_prefs_url(@member) %>
