The thread "<%= raw @thread.title %>" has an upcoming deadline regarding
"<%= raw @deadline_message.title %>" at
<%= raw @deadline_message.deadline.to_formatted_s(:long_ordinal) %>

You can view the thread at:
<%= thread_url(@thread) %>

--------

You can stop following this thread by visiting the link above and clicking 'unfollow this thread'.
