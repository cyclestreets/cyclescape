.tags-panel
  .tags
    %ul
      - thread.tags.each do |tag|
        %li.tag= tag.name
    = link_to t(".edit_tags"), "#", rel: "edit-tags"
  = render partial: "message_thread/tags/edit", locals: {thread: thread}
