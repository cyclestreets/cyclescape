<%# frozen_string_literal: true
 %>

--------
<%= t(".thread", application_name: @site_config.application_name) %>:
<% if defined?(@message) %>
  <%= thread_url(@thread, anchor: dom_id(@message)) %>
<% else %>
  <%= thread_url(@thread) %>
<% end %>
You can reply to the email and the message will be added to the thread.

<% if @thread.private_to_committee? %>
Only committee members of <%= raw @thread.group.name %> can view
and post messages to this discussion thread.
<% elsif @thread.private_to_group? %>
Only members of <%= raw @thread.group.name %> can view
and post messages to this discussion thread.
<% elsif @thread.user %>
This is a Private Message between <%= raw @thread.created_by.name %>
and <%= raw @thread.user.name %>.
<% else %>
Everyone can view this discussion thread and post messages.
<% end %>

<%= t ".unsubscribe" %>
<%= edit_thread_subscription_url(@thread, @subscription, t: @subscriber.public_token) %>
