<%= render partial: 'header', locals: {message: @message, type: t('.type')} %>

<%= raw @message.component.item.title %>

<%= raw polymorphic_url @message.component.item.component %>

<%= raw @message.body %>
