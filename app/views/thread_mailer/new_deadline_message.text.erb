<%= @message.created_by.name %> added a deadline to the thread

<%= l @message.component.deadline.to_date, format: :long %>

<%= @message.component.title %>

--------
Cyclescape thread: <%= thread_url(@message.thread) %>
You can unsubscribe on the thread page.
