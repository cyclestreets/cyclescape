%article.thread.widget
  .wrapper
    %header
      %h3= link_to_if policy(thread).show?, thread.title, thread_path(thread)
      %span.privacy= thread_type(thread) + ", "
      %span.count= pluralize(thread.messages.count, "message") + ", "
      %span.participants= pluralize(thread.participants.count(:id), "participant")
    %section.messages
      = render collection: thread.messages.recent, partial: "messages/message", locals: {thread: thread}
