%section.library.box
  %h2= t ".library"
  - if thread_tags.empty?
    %p= t ".tag_thread_guidance"
  - elsif items.empty?
    %p= t ".no_library_items"
  - else
    %ul
      - items.each do |item|
        %li= link_to item.title, item.component
