%section#content.content-no-sidebar
  .user-dashboard#dashboard-tabs
    %ul.tabs.styled-tabs.with-history
      %li= link_to t(".my_threads"), "#my-threads"
      %li= link_to t(".issues_nearby"), "#issues-nearby"
      %li= link_to t(".my_priorities"), "#my-priorities"
      %li= link_to t(".deadlines"), "#deadlines"
      %li= link_to t(".planning_applications"), "#planning-applications"

    .panes
      #my-threads
        %section.threads
          - if @subscribed_threads.empty?
            %p= t ".no_subscribed_threads_html", threads_link: link_to(t(".recent_threads"), threads_path),
                                          add_issue_link: link_to(t(".add_a_new_issue"), new_issue_path)
          - else
            %p= t(".my_threads_introduction")
            %table.dashboard-threads
              %tbody
                - @subscribed_threads.each do |thread|
                  %tr
                    %td.title
                      %h4= link_to thread.title, thread_path(thread)
                      - if thread.has_issue?
                        %p= thread.issue_link
                    %td.activity
                      = thread.latest_activity
                    %td.meta
                      = time_tag_with_title(thread.latest_activity_date) do
                        - t ".thread_latest_activity", time_ago: time_ago_in_words(thread.latest_activity_date)
        #dashboard-threads-issues
          %section#group-threads
            %h2= t(".group_threads")
            - if current_user.groups.empty?
              %p= t ".no_user_groups"
            - elsif @group_threads.empty?
              %p= t ".no_group_threads"
            - else
              %ul.group-threads-list
                - @group_threads.each do |thread|
                  %li
                    %span
                    = link_to_profile thread.group, class: "group-link"
                    %div
                      %h4= link_to thread.title, thread_path(thread)
      #issues-nearby
        %section.issues
          %h2= t ".issues_in_your_area"
          - if current_user.locations.empty?
            %p.warning
              = image_tag 'warning.png'
              = t ".no_locations_guidance_html", add_some_locations_link: link_to(t(".add_some_locations"), user_locations_path),
                                                  things: t(".issues_lower")
          - elsif @relevant_issues.empty?
            %p= t ".no_issues_nearby_html", add_issue_link: link_to(t(".add_a_new_issue"), new_issue_path),
                                        add_location_link: link_to(t(".add_another_location"), user_locations_path)
          - else
            %p= t ".my_issues_nearby_introduction_html", areas_you_are_watching_link: link_to(t(".areas_you_are_watching"), user_locations_path)
            %p.comment= t ".my_issues_nearby_issues_pointer_html", browse_all_other_issues_link: link_to(t(".browse_all_other_issues"), issues_path)
            %ul.issue-list
              = issues_list @relevant_issues, collapsed: true
      #my-priorities
        %section.threads
          - if @prioritised_threads.empty?
            %p= t ".no_prioritised_threads_html", threads_link: link_to(t(".recent_threads"), threads_path),
                                          add_issue_link: link_to(t(".add_a_new_issue"), new_issue_path)
          - else
            %p= t ".my_priorities_introduction"
            %table.dashboard-threads
              %tbody
                - @prioritised_threads.each do |thread|
                  %tr
                    %td.title
                      %h4= link_to thread.title, thread_path(thread)
                      - if thread.has_issue?
                        %p= thread.issue_link
                    %td.activity
                      = thread.latest_activity
                    %td.meta
                      = t "thread_priorities.#{thread.priority_for(current_user).label}"
      #deadlines
        %section.threads
          - if @deadline_threads.empty?
            %p= t ".no_upcoming_deadline_threads"
          - else
            %p= t ".my_upcoming_deadline_introduction"
            %table.dashboard-threads
              %tbody
                - @deadline_threads.each do |thread|
                  %tr
                    %td.title
                      %h4= link_to thread.title, thread_path(thread)
                      - if thread.has_issue?
                        %p= thread.issue_link
                    %td.activity
                      %ul.content-icon-list
                        - thread.upcoming_deadline_messages.each do |message|
                          %li.cal
                            %h4= l message.component.deadline.to_date, format: :long
                            %p= message.component.title
                            - deadline = message.component.deadline.to_date
                            - diff = deadline - DateTime.now.to_date
                            - if diff >= 0 && diff < 30
                              %p.warning= t ".days_away", count: diff.to_i
                    %td.meta
                      = time_tag_with_title(thread.latest_activity_date) do
                        - t ".deadline_thread_updated", time_ago: time_ago_in_words(thread.latest_activity_date)
      #planning-applications
        %section.planning_applications
          %h2= t ".planning_applications_in_your_area", location: t('.location', count: current_user.locations.count)
          = form_tag(search_planning_applications_path, {method: :get, class: "filters"}) do
            = text_field_tag :q, params[:q], placeholder: t(".planning_applications_search")
            = button_tag t(".planning_application_search_button")

          - if current_user.locations.empty?
            %p.warning
              = image_tag 'warning.png'
              = t ".no_locations_guidance_html", add_some_locations_link: link_to(t(".add_some_locations"), user_locations_path),
                                                  things: t(".planning_applications_lower")
          - elsif @planning_applications.empty?
            %p= t ".no_planning_applications_nearby_html", add_location_link: link_to(t(".add_another_location"), user_locations_path)
          - else
            %p= t ".planning_applications_introduction_html", location_link: link_to(t(".location", count: current_user.locations.count), user_locations_path)

            - if @planning_applications.respond_to?(:total_pages)
              = will_paginate @planning_applications, param_name: :planning_page, params: {anchor: 'planning-applications'}
            %br
            %br

            %table.dashboard-planning-applications.listing
              %tbody
                = render partial: 'planning_applications/planning_application', collection: @planning_applications, as: :planning_application

        - if @planning_applications.respond_to?(:total_pages)
          = will_paginate @planning_applications, param_name: :planning_page, params: {anchor: 'planning-applications'}
