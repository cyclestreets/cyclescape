= t(".resolves_actions")
%ul{style: "padding-left: 12px"}
  - actions.each do |action|
    %li{style: "list-style-type: disc"}= link_to action.description, thread_path(action.message.thread_id, anchor: dom_id(action.message))
