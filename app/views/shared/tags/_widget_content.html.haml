%div{id: dom_id(resource, :tags)}
  %ul{class: "tags"}
    - resource.tags.each do |tag|
      %li{class: "tag"}= link_to tag.name, tag_path(tag)
  - if policy(resource).update_tags?
    = link_to t(".edit_tags"), edit_polymorphic_path([resource, :tag]), data: { turbo: true }
