#new-library-item-message.pane
  - library = message.library_item_messages.build
  = message_form.semantic_fields_for :library_item_messages, library do |f|
    = f.inputs do
      = f.input :item
      = semantic_fields_for Message.new do |f2|
        = f2.input :body
    = f.actions do
      = f.action :submit, button_html: message_button_html
