#new-link-message
  = semantic_form_for LinkMessage.new, url: thread_links_path(@thread) do |f|
    = f.inputs do
      = f.input :url, as: :string
      = f.input :title
      = f.input :description
    = f.buttons do
      = f.commit_button
