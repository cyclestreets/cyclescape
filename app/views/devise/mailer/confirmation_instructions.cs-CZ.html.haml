-# frozen_string_literal: true

%p
  Vítejte, #{@resource.email}
%p Váš účet můžete potvrdit pomocí následujícího odkazu:
%p= link_to 'Potvrdit účet', confirmation_url(@resource, :confirmation_token => @token)
