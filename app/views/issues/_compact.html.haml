%li
  %article.issue
    .map
      - m = tiny_display_map(issue, geometry_issue_path(issue, :json), "list") do |map, page, dom_id|
        %div{id: dom_id, class: "tinymap"}
      != m.to_html
    .issue-content
      %h3= link_to issue.title, issue
      .meta
        %p
          = issue.creator_link
          = issue.creation_time
          \//
          = t(".thread_count", count: issue.thread_count)
        .tags
          = issue.tags_list
      %p
        = issue.brief_description
    .votes-cell
      .votes
        .vote-count
          %span= issue.vote_count
          = t(".votes")
